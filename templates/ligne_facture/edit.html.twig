<!DOCTYPE html>

<title>Edit LigneFacture</title>

{% block body %}
    <h1>Edit LigneFacture</h1>

    {{ include('ligne_facture/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_ligne_facture_index') }}">back to list</a>

    {{ include('ligne_facture/_delete_form.html.twig') }}
{% endblock %}
